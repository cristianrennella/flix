%section.people
  %header
    %h2 People I Follow
  %table.table
    %thead
      %tr
        %th(width="30%")
        %th(width="20%") Videos in Queue
        %th(width="20%") Followers
        %th(width="30%") Unfollow
    %tbody
      - @user.following.each do |following|
        %tr
          %td
            %img(src="http://www.gravatar.com/avatar/#{Digest::MD5.hexdigest('#{following.email}'.downcase)}?s=40")
            = link_to following.username, following
          %td.extra-padding= following.queue_items.count
          %td.extra-padding= following.following.count
          %td.extra-padding
            = link_to "", method: :post do
              %i.glyphicon.glyphicon-remove